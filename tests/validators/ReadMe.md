# Validators Tests

These tests cover shared validation helpers in `src/validators`:
- `phone_number.py`: phone number validation and E.164 formatting

## How to run
From the repository root:

```bash
pytest tests/validators
```

From the module folder:

```bash
cd tests/validators
pytest .
```
